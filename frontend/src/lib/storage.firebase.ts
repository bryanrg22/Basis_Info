/**
 * Mock Firebase Storage Service
 * 
 * Re-exports the mock storage service for compatibility with
 * imports that expect the Firebase storage module.
 */

export { storageService } from './storage';

